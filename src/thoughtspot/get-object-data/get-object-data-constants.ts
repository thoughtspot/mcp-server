// Object types get_object_data supports. Single source for the Zod schema
// (tool-definitions) and the endpoint switch (get-object-data).
export const GET_OBJECT_DATA_SUPPORTED_TYPES = ["ANSWER", "LIVEBOARD"] as const;
